numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# Optional: enables real Claude-powered SQL drafting and answer synthesis.
# Without this installed (or without ANTHROPIC_API_KEY set), the project
# uses dynamic rule-based SQL parsing and hybrid retrieval end-to-end offline.
anthropic
